Each node has a node name (device name)
Each relay has a relay name, which should tell what kind of appliance this relay controls
Mqtt message data fields include a command and a relay name in the form "reln:cmd"
When multiple relays on different devices have the same name, the command is executed for every single one.
A single relay can have multiple names.

MQTT structure:
Universal and local relay commands
Status reports

